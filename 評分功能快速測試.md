# 評分功能 - 快速測試指南

## 🚀 立即測試

### 方法 1: 測試手動評分按鈕（最簡單）

1. **啟動 App**
2. **點擊「設定」**（齒輪圖示）
3. **滾動到最下方**「關於」區塊
4. **找到「評分此 App」**（⭐️ 星星圖示）
5. **點擊它**
6. ✅ 應該會看到 iOS 原生的評分對話框

### 方法 2: 測試自動評分提示

由於自動評分需要符合條件，建議在開發環境中測試：

#### 在 Xcode 中加入測試程式碼

在 `ContentView.swift` 的 `onAppear` 中加入（僅用於測試）：

```swift
.onAppear {
    // ... 現有程式碼
    
    #if DEBUG
    // === 測試用：查看目前統計 ===
    let stats = AppRatingManager.shared.getStatistics()
    print("📊 評分統計:", stats)
    
    // === 測試用：重置評分狀態（可選） ===
    // 如果想重新測試，取消下面這行的註解
    // AppRatingManager.shared.resetRatingStatus()
    #endif
}
```

#### 觸發自動評分的步驟

1. **啟動 App 5 次**
   - 關閉並重新開啟 App 5 次
   - 每次啟動都會增加計數

2. **查看 10 個詳情頁面**
   - 點擊任何歌曲 → 進入詳情頁面
   - 返回 → 點擊另一首歌曲
   - 重複 10 次（可以是歌曲、專輯或藝人）

3. **第 11 次查看詳情時**
   - 系統會自動顯示評分對話框

## 📱 預期結果

### 成功顯示評分對話框

評分對話框應該顯示：
- ⭐️⭐️⭐️⭐️⭐️ 五顆星
- "您喜歡 spo.stats 嗎？" 或類似文字
- "提交" 和 "不用了，謝謝" 按鈕

### 在模擬器中測試的限制

⚠️ **注意**: 在 iOS 模擬器中，評分對話框可能不會完全顯示，這是正常的。建議在**真實設備**上測試以獲得最佳效果。

## 🔍 Debug 檢查清單

### 檢查 Console 輸出

執行 App 時，應該會看到這些日誌：

```
📱 App 啟動次數: 1
📱 App 啟動次數: 2
...
⭐️ 顯著事件次數: 1
⭐️ 顯著事件次數: 2
...
🎯 記錄事件: viewedTrackDetail
🎯 記錄事件: viewedArtistDetail
```

### 查看統計資訊

在 Xcode Console 中應該看到：

```
📊 評分統計: [
    "啟動次數": 5,
    "顯著事件次數": 10,
    "已評分": false,
    "上次請求日期": "從未",
    "距離上次請求天數": <最大值>
]
```

## 🛠️ 快速重置（測試用）

如果想重新測試自動評分，在 `ContentView.swift` 中加入：

```swift
#if DEBUG
Button("重置評分狀態") {
    AppRatingManager.shared.resetRatingStatus()
    print("✅ 已重置！可以重新測試")
}
#endif
```

或直接在程式碼中呼叫：

```swift
#if DEBUG
AppRatingManager.shared.resetRatingStatus()
#endif
```

## ✅ 測試檢查清單

- [ ] 手動評分按鈕在設定頁面正確顯示
- [ ] 點擊評分按鈕會顯示評分對話框
- [ ] 啟動 App 5 次後，啟動計數正確
- [ ] 查看 10 個詳情頁面後，事件計數正確
- [ ] 條件滿足後，自動顯示評分對話框
- [ ] 評分對話框可以正常關閉
- [ ] 評分後不會再次顯示（除非重置）
- [ ] 多語言文字正確顯示（繁中、英文等）

## 🎯 最快測試路徑

**只需要 2 步驟即可看到評分功能**：

1. ▶️ 執行 App
2. ⚙️ 設定 → 滾動到底部 → 點擊「評分此 App」

就這麼簡單！ 🎉

## 📞 遇到問題？

### 問題 1: 點擊評分按鈕沒反應

**可能原因**:
- 在模擬器中測試（建議用真實設備）
- StoreKit 未正確載入

**解決方案**:
- 在真實 iPhone 或 iPad 上測試
- 確保已連接網路

### 問題 2: 自動評分沒有顯示

**可能原因**:
- 未達到觸發條件
- iOS 系統限制（每年最多 3 次）

**解決方案**:
- 檢查 Console 日誌確認計數
- 使用 `resetRatingStatus()` 重置狀態

### 問題 3: 找不到評分按鈕

**檢查**:
- 確保已啟動 App
- 進入「設定」頁面
- 滾動到「關於」區塊（最下方）
- 評分按鈕在「開發者」資訊下方

## 💡 提示

- 🚀 在真實設備上測試效果最好
- 🔍 開啟 Console 查看詳細日誌
- ⏱️ 自動評分需要時間累積，不用急
- 🎨 評分對話框的外觀由系統決定，無法自訂

---

**祝測試順利！** 🎉

