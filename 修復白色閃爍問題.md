# 修復白色閃爍問題 🔧

## ✅ 已完成的修改

### 1. Launch Screen.storyboard
- ✅ 背景色改為深灰色 RGB(0.07, 0.07, 0.07) = #121212
- ✅ 移除所有文字元素（綠色 "MyPlaylist" 等）
- ✅ 現在是純深色背景

### 2. Info.plist
- ✅ 添加 `UIUserInterfaceStyle` = `Dark`（強制深色模式）
- ✅ 添加 `UILaunchStoryboardName` = `Launch Screen`（指定 Launch Screen）

---

## 🛠️ 必須執行的步驟

白色閃爍通常是因為 Xcode 快取了舊的 Launch Screen。請按照以下步驟**徹底清除快取**：

### 步驟 1: 清除 Xcode 快取
```bash
# 在終端機執行以下命令
rm -rf ~/Library/Developer/Xcode/DerivedData/*
```

### 步驟 2: 清除 App 快取
在 Xcode 中：
1. 選擇選單 **Product** → **Clean Build Folder**（或按 Shift + Cmd + K）
2. 等待清理完成

### 步驟 3: 刪除 App
1. 在模擬器或實體裝置上**長按 App 圖示**
2. 選擇**移除 App**
3. 確認刪除

### 步驟 4: 重新編譯並安裝
1. 在 Xcode 中重新編譯專案（Cmd + B）
2. 運行 App（Cmd + R）

### 步驟 5: 重新啟動裝置（可選但建議）
如果上述步驟無效，請：
- **模擬器**：Device → Erase All Content and Settings
- **實體裝置**：完全關機後重新開機

---

## 🔍 驗證方式

完成上述步驟後，測試是否還有白色閃爍：

1. **完全關閉 App**（從 App 切換器上滑關閉）
2. **重新點擊 App 圖示開啟**
3. 觀察啟動過程

應該看到的流程：
```
點擊 App 圖示
    ↓
深灰色背景（Launch Screen）
    ↓
深灰色背景 + SpoStats 動畫（LaunchScreenView）
    ↓
主畫面
```

**完全沒有任何白色閃爍！**

---

## 🎯 替代方案

如果上述方法都無效，可能需要檢查 Xcode 專案設定：

### 檢查 1: 專案 Launch Screen 設定
1. 在 Xcode 中選擇專案
2. 選擇 **Target** → **MyPlaylist**
3. 切換到 **Info** 標籤
4. 確認 **Launch Screen File** 設定為 `Launch Screen`

### 檢查 2: Asset Catalog 設定
1. 打開 **Assets.xcassets**
2. 檢查是否有 **LaunchImage** 相關的資源
3. 如果有，刪除它們（因為我們使用 Storyboard）

### 檢查 3: 使用純色背景（終極方案）
如果 Storyboard 仍有問題，可以改用 SwiftUI 的方式：

在 `MyPlaylistApp.swift` 中設定背景色：
```swift
@main
struct MyPlaylistApp: App {
    @UIApplicationDelegateAdaptor(AppDelegate.self) var appDelegate
    
    init() {
        // 設定 Launch Screen 背景色
        UIView.appearance(for: UITraitCollection(userInterfaceStyle: .dark)).backgroundColor = UIColor(red: 0.07, green: 0.07, blue: 0.07, alpha: 1.0)
    }

    var body: some Scene {
        WindowGroup {
            ContentView()
                .preferredColorScheme(.dark)
        }
    }
}
```

---

## 📱 iOS 特殊情況

### iOS 會快取 Launch Screen

iOS 系統會**永久快取** Launch Screen，所以：

1. **修改 Launch Screen 後必須刪除 App 重裝**
2. **有時甚至需要重新啟動裝置**
3. **模擬器可能需要 "Erase All Content and Settings"**

這是 iOS 的已知行為，不是 bug。

### 開發階段快速測試方法

在開發階段，每次修改 Launch Screen 後：
```bash
# 清除快取、刪除 App、重新安裝
rm -rf ~/Library/Developer/Xcode/DerivedData/* && 
echo "快取已清除，請在 Xcode 中重新運行 App"
```

---

## 🎨 顏色對照

確保以下顏色完全一致：

| 位置 | 顏色值 | 說明 |
|------|--------|------|
| Launch Screen.storyboard | RGB(0.07, 0.07, 0.07) | 系統啟動畫面 |
| Color.spotifyText | RGB(0.07, 0.07, 0.07) | SwiftUI 顏色 |
| LaunchScreenView 背景 | Color.spotifyText | 自定義啟動畫面 |
| ContentView 背景 | Color.spotifyText | 主畫面 |

所有背景都是 **#121212**（RGB: 18, 18, 18）

---

## ✨ 預期效果

完成所有設定後，您的 App 啟動應該：
- ✅ 完全沒有白色閃爍
- ✅ 從頭到尾都是深色背景
- ✅ 平滑過渡到自定義啟動畫面
- ✅ 品牌動畫流暢顯示

---

## 💡 常見問題

### Q: 為什麼刪除 App 後還是白色？
A: iOS 可能還記住了快取，請重新啟動裝置。

### Q: 模擬器和實機表現不同？
A: 正常現象。模擬器快取更頑固，建議 Erase All Content。

### Q: 是否需要修改 Assets.xcassets？
A: 不需要。我們使用 Storyboard，不使用 Asset Catalog 的 Launch Image。

### Q: UIUserInterfaceStyle = Dark 會影響用戶設定嗎？
A: 會強制深色模式，但這符合您的 App 設計（始終深色主題）。

---

## 📞 如果問題持續

如果執行所有步驟後仍有白色閃爍：

1. 檢查 Xcode 專案設定中的 Launch Screen
2. 確認沒有其他 Launch Image 資源
3. 嘗試建立新的 Launch Screen.storyboard
4. 檢查是否有第三方框架干擾

---

**記住**：修改 Launch Screen 後，**一定要刪除 App 並重裝**才能看到效果！ 🎯

