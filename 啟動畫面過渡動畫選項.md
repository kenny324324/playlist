# 啟動畫面過渡動畫選項 🎬

## 目前實作：淡入淡出 (Fade/Opacity)

```swift
// 目前在 ContentView.swift 中的實作
if showLaunchScreen {
    LaunchScreenView(...)
        .transition(.opacity)  // ← 淡入淡出效果
}
```

## 可用的過渡動畫選項

### 1️⃣ **淡入淡出** (Fade) - 目前使用
```swift
.transition(.opacity)
```
- **效果**：啟動畫面逐漸淡出，主畫面逐漸淡入
- **適用場景**：最常見，優雅柔和
- **速度**：0.4 秒

---

### 2️⃣ **縮放淡出** (Scale + Fade)
```swift
.transition(.scale(scale: 1.2).combined(with: .opacity))
```
- **效果**：啟動畫面放大同時淡出
- **適用場景**：有膨脹感，現代化
- **速度**：0.4 秒

---

### 3️⃣ **向上滑動** (Slide Up)
```swift
.transition(.move(edge: .top))
```
- **效果**：啟動畫面向上滑出，主畫面從下方升起
- **適用場景**：類似揭開序幕
- **速度**：0.5 秒

---

### 4️⃣ **向上滑動 + 淡出** (Slide Up + Fade)
```swift
.transition(.asymmetric(
    insertion: .move(edge: .bottom).combined(with: .opacity),
    removal: .move(edge: .top).combined(with: .opacity)
))
```
- **效果**：啟動畫面向上滑出並淡出，主畫面從下方升起並淡入
- **適用場景**：流暢自然的轉場
- **速度**：0.5 秒

---

### 5️⃣ **縮小淡出** (Scale Down + Fade)
```swift
.transition(.scale(scale: 0.8).combined(with: .opacity))
```
- **效果**：啟動畫面縮小並淡出（像是被吸入）
- **適用場景**：聚焦感，引人注意
- **速度**：0.4 秒

---

### 6️⃣ **模糊淡出** (Blur + Fade) - 需自訂
```swift
.transition(AnyTransition.modifier(
    active: BlurModifier(radius: 20),
    identity: BlurModifier(radius: 0)
).combined(with: .opacity))
```
- **效果**：啟動畫面逐漸模糊並淡出
- **適用場景**：高級感，類似 iOS 原生動畫
- **速度**：0.5 秒
- **注意**：需要額外定義 BlurModifier

---

### 7️⃣ **旋轉縮小淡出** (Rotate + Scale + Fade)
```swift
.transition(AnyTransition.scale(scale: 0.5)
    .combined(with: .opacity)
    .animation(.spring(response: 0.6, dampingFraction: 0.8)))
```
- **效果**：啟動畫面旋轉縮小並淡出
- **適用場景**：趣味性、創意感
- **速度**：0.6 秒（帶彈簧效果）

---

### 8️⃣ **無動畫** (Instant)
```swift
.transition(.identity)
```
- **效果**：立即切換，無動畫
- **適用場景**：快速啟動，減少等待感
- **速度**：0 秒

---

## 如何修改

### 方法一：在 ContentView.swift 中直接修改

找到這段程式碼：
```swift
if showLaunchScreen {
    LaunchScreenView(...)
        .transition(.opacity)  // ← 修改這裡
}
```

替換成你想要的過渡效果，例如：
```swift
if showLaunchScreen {
    LaunchScreenView(...)
        .transition(.scale(scale: 1.2).combined(with: .opacity))
}
```

### 方法二：添加個性化設定選項

可以在設定頁面讓使用者選擇喜歡的過渡動畫：

```swift
// 在 SettingsView 添加選項
enum LaunchTransition: String, CaseIterable {
    case fade = "淡入淡出"
    case scaleUp = "放大淡出"
    case slideUp = "向上滑動"
    case scaleDown = "縮小淡出"
    
    var transition: AnyTransition {
        switch self {
        case .fade:
            return .opacity
        case .scaleUp:
            return .scale(scale: 1.2).combined(with: .opacity)
        case .slideUp:
            return .move(edge: .top)
        case .scaleDown:
            return .scale(scale: 0.8).combined(with: .opacity)
        }
    }
}

@AppStorage("launchTransition") private var selectedTransition: String = "fade"
```

---

## 推薦組合

### 🌟 最優雅
```swift
.transition(.opacity)
```
簡單、專業、不會搶戲

### 🚀 最現代
```swift
.transition(.scale(scale: 1.2).combined(with: .opacity))
```
有動感但不誇張

### 💎 最高級
```swift
.transition(.asymmetric(
    insertion: .opacity,
    removal: .scale(scale: 1.1).combined(with: .opacity)
))
```
啟動畫面放大淡出，主畫面淡入

### ⚡ 最快速
```swift
.transition(.identity)
```
不浪費任何時間

---

## 自訂動畫時長

如果想調整動畫速度，可以在 `withAnimation` 中設定：

```swift
// 在 LaunchScreenView.swift 的 startLoading() 中
withAnimation(.easeInOut(duration: 0.4)) {  // ← 調整這裡
    onLoadingComplete()
}
```

常見時長：
- **快速**：0.2-0.3 秒
- **標準**：0.4-0.5 秒（目前）
- **緩慢**：0.6-0.8 秒
- **很慢**：1.0 秒以上

---

## 當前配置總結

✅ **配色**：已使用使用者選擇的主題色 (`themeManager.themeColor`)
✅ **名稱**：已改為 "SpoStats"
✅ **Icon**：已使用真實的 App Icon
✅ **過渡**：目前使用淡入淡出 (`.opacity`)

如果您想更換過渡動畫，只需要在 `ContentView.swift` 第 50 行修改 `.transition(.opacity)` 即可！

