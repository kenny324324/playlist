# App 評分功能實作說明

## 📱 功能概述

成功為您的應用程式加入完整的評分功能，包含：

1. **手動評分按鈕** - 在設定頁面的「關於」區塊
2. **智能評分提示** - 在適當時機自動顯示評分請求

## ✅ 已完成的實作

### 1. AppRatingManager (評分管理器)

**檔案位置**: `MyPlaylist/Services/AppRatingManager.swift`

**功能**:
- 記錄 App 啟動次數
- 追蹤用戶的顯著事件（查看詳情頁面等）
- 智能判斷何時顯示評分請求
- 使用 iOS 原生 `SKStoreReviewController` 顯示評分對話框

**評分觸發條件**:
- ✅ 至少啟動 **5 次**
- ✅ 至少產生 **10 次顯著事件**（查看歌曲/專輯/藝人詳情）
- ✅ 距離上次請求至少 **30 天**
- ✅ 用戶未曾評分過

**顯著事件類型**:
```swift
- viewedTrackDetail      // 查看歌曲詳情
- viewedArtistDetail     // 查看藝人詳情
- viewedAlbumDetail      // 查看專輯詳情
- sharedContent          // 分享內容
- viewedDashboard        // 查看儀表板
- viewedStats            // 查看統計資料
- changedTheme           // 更改主題
- enabledNotification    // 啟用通知
```

### 2. 設定頁面整合

**檔案位置**: `MyPlaylist/Views/SettingsView.swift`

**修改內容**:
- 在「關於」區塊加入 **「評分此 App」** 按鈕
- 按鈕圖示：⭐️ `star.fill`
- 點擊後立即觸發原生評分對話框

**位置**: 在「開發者」資訊下方

### 3. 自動記錄整合

#### App 啟動記錄
**檔案位置**: `MyPlaylist/Views/ContentView.swift`

```swift
.onAppear {
    // ... 其他程式碼
    
    // 記錄 App 啟動（用於評分提示）
    AppRatingManager.shared.recordAppLaunch()
}
```

#### 詳情頁面事件記錄

**TrackDetailView** (`MyPlaylist/Views/TrackDetailView.swift`):
```swift
.onAppear {
    refreshAccessTokenAndLoad()
    // 記錄顯著事件（查看歌曲詳情）
    AppRatingManager.shared.recordEvent(.viewedTrackDetail)
}
```

**ArtistDetailView** (`MyPlaylist/Views/ArtistDetailView.swift`):
```swift
.onAppear {
    refreshAccessTokenAndLoad()
    // 記錄顯著事件（查看藝人詳情）
    AppRatingManager.shared.recordEvent(.viewedArtistDetail)
}
```

**AlbumDetailView** (`MyPlaylist/Views/AlbumDetailView.swift`):
```swift
.onAppear {
    refreshAccessTokenAndLoad()
    // 記錄顯著事件（查看專輯詳情）
    AppRatingManager.shared.recordEvent(.viewedAlbumDetail)
}
```

### 4. 本地化支援

**檔案位置**: `MyPlaylist/Localizable.xcstrings`

已加入 `settings.rateApp` 的多語言支援：
- 🇹🇼 繁體中文: "評分此 App"
- 🇺🇸 英文: "Rate This App"
- 🇯🇵 日文: "このアプリを評価"
- 🇰🇷 韓文: "앱 평가하기"

## 🧪 測試指南

### 手動評分測試

1. 執行 App
2. 進入「設定」頁面
3. 滾動到「關於」區塊
4. 點擊 **「評分此 App」**
5. 應該會彈出原生的評分對話框

### 自動評分提示測試

由於需要符合多個條件，建議使用 Debug 功能：

```swift
#if DEBUG
// 在開發模式下重置評分狀態（用於測試）
AppRatingManager.shared.resetRatingStatus()

// 查看當前統計資訊
print(AppRatingManager.shared.getStatistics())
#endif
```

**測試步驟**:
1. 啟動 App 5 次以上
2. 查看至少 10 個不同的詳情頁面（歌曲/專輯/藝人）
3. 當條件滿足時，系統會在下次查看詳情頁面時自動顯示評分對話框

## 🎯 評分策略說明

### 為什麼使用 SKStoreReviewController？

- ✅ **Apple 官方推薦**的評分方式
- ✅ **原生體驗**，符合 iOS 設計規範
- ✅ **自動限制**頻率（系統每年最多顯示 3 次）
- ✅ **用戶友善**，可以直接在 App 內完成
- ✅ **不強制**用戶評分，可以選擇關閉

### 智能提示的好處

1. **不打擾用戶** - 只在用戶充分使用 App 後才提示
2. **提高評分率** - 在用戶有良好體驗時請求評分
3. **避免頻繁提示** - 30 天間隔確保不會煩擾用戶
4. **自然時機** - 在查看詳情等正向互動後提示

## 📊 評分數據追蹤

所有數據都儲存在 `UserDefaults` 中：
- `appLaunchCount` - App 啟動次數
- `significantEventCount` - 顯著事件次數
- `lastRatingRequestDate` - 上次請求日期
- `userHasRatedApp` - 用戶是否已評分

## 🔧 進階配置

如需調整評分條件，可以修改 `AppRatingManager.swift` 中的參數：

```swift
private enum Criteria {
    static let minimumLaunchCount = 5           // 最少啟動次數
    static let minimumDaysSinceLastRequest = 30 // 請求間隔天數
    static let significantEventThreshold = 10   // 顯著事件門檻
}
```

## 🚀 上架前注意事項

### App Store 資訊更新

**重要**: 目前程式碼中的 App Store URL 需要更新：

```swift
// 在 AppRatingManager.swift 中
func openAppStoreRating() {
    // 將 YOUR_APP_ID 替換成實際的 App Store ID
    guard let appStoreURL = URL(string: "https://apps.apple.com/app/idYOUR_APP_ID?action=write-review") else {
        return
    }
    UIApplication.shared.open(appStoreURL)
}
```

**如何取得 App ID**:
1. 登入 [App Store Connect](https://appstoreconnect.apple.com/)
2. 選擇您的 App
3. 在「App 資訊」頁面找到 Apple ID（純數字）
4. 替換程式碼中的 `YOUR_APP_ID`

### Apple 審核建議

- ✅ 使用原生 `SKStoreReviewController` 符合 Apple 審核規範
- ✅ 不會強制用戶評分
- ✅ 不會過度頻繁提示
- ✅ 提供清晰的手動評分選項

## 💡 未來擴展建議

可以在其他適當時機記錄顯著事件，例如：

1. **分享功能** - 當用戶分享內容時
   ```swift
   AppRatingManager.shared.recordEvent(.sharedContent)
   ```

2. **主題變更** - 當用戶自訂主題時
   ```swift
   AppRatingManager.shared.recordEvent(.changedTheme)
   ```

3. **啟用通知** - 當用戶開啟通知時
   ```swift
   AppRatingManager.shared.recordEvent(.enabledNotification)
   ```

4. **查看儀表板** - 當用戶查看統計資料時
   ```swift
   AppRatingManager.shared.recordEvent(.viewedDashboard)
   ```

## 📝 總結

✅ **完整實作** - 包含手動和自動評分功能
✅ **智能觸發** - 基於用戶行為的智能評分提示
✅ **用戶友善** - 不打擾用戶體驗
✅ **符合規範** - 遵循 Apple 審核指南
✅ **多語言支援** - 支援繁中、英文、日文、韓文
✅ **易於擴展** - 可輕鬆加入更多觸發事件

## 🔗 相關文件

- [Apple 官方文檔 - SKStoreReviewController](https://developer.apple.com/documentation/storekit/skstorereviewcontroller)
- [App Store 評分和評論指南](https://developer.apple.com/app-store/ratings-and-reviews/)

---

**建立日期**: 2024-11-26
**版本**: 1.0

