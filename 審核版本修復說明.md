# 審核版本修復說明

## 🔧 修改內容

**修改日期**：2025-11-12  
**問題**：Apple 審核團隊報告「The login screen was not available」  
**根本原因**：審核版本顯示了兩個按鈕，審核員點擊了綠色登入按鈕而非 Demo Mode 按鈕

### 修改的檔案

```
MyPlaylist/Views/HomeView.swift
```

### 具體修改

在 **toolbar 的登入按鈕** 外層加上條件編譯：

```swift
#if !APPSTORE_REVIEW
// 登入按鈕代碼
#endif
```

**效果**：
- ✅ **審核版本**：左上角 toolbar **不顯示**綠色登入按鈕
- ✅ **正式版本**：左上角 toolbar **正常顯示**綠色登入按鈕

---

## ✅ 驗證步驟

### 步驟 1：測試審核版本（應該隱藏登入按鈕）

```bash
# 1. 在 Xcode 切換到審核 Scheme
Product → Scheme → MyPlaylist (AppStore Review)

# 2. 確認當前 Scheme（Xcode 頂部工具欄）
應該顯示：MyPlaylist (AppStore Review)

# 3. 運行 App
⌘R (Command + R)

# 4. 檢查 App 界面
✅ 預期結果：
   - 左上角 toolbar：無任何按鈕（空白）
   - 頁面中央：橘色「🎭 Demo Mode (For Review)」按鈕
   - 右上角：刷新按鈕（圓形箭頭）

❌ 如果看到左上角有綠色「Log In」按鈕 = 有問題！
```

### 步驟 2：測試正式版本（應該顯示登入按鈕）

```bash
# 1. 在 Xcode 切換到正式 Scheme
Product → Scheme → MyPlaylist

# 2. 運行 App
⌘R (Command + R)

# 3. 檢查 App 界面
✅ 預期結果：
   - 左上角 toolbar：綠色「Log In」按鈕
   - 頁面中央：無 Demo Mode 按鈕（因為 #if APPSTORE_REVIEW 為 false）
   - 右上角：刷新按鈕

❌ 如果左上角沒有登入按鈕 = 有問題！
```

### 步驟 3：測試 Demo Mode 功能

```bash
# 使用審核版本 Scheme
1. 運行審核版本
2. 點擊橘色「Demo Mode (For Review)」按鈕
3. 檢查：
   ✅ 頂部出現橘色「🎭 Demo Mode 🎭」標籤
   ✅ 頁面載入模擬數據（歌曲、藝人等）
   ✅ 所有功能可正常使用
```

---

## 📱 審核版本 vs 正式版本對比

| 項目 | 審核版本 | 正式版本 |
|------|---------|---------|
| **左上角 toolbar** | 無按鈕（空白）✅ | 綠色「Log In」按鈕 ✅ |
| **Demo Mode 按鈕** | 顯示（橘色）✅ | 不顯示 ✅ |
| **Demo 模式標籤** | 進入後顯示 ✅ | 不存在 ✅ |
| **編譯標誌** | APPSTORE_REVIEW ✅ | 無特殊標誌 ✅ |

---

## 🎯 給審核團隊的新說明

修改後，建議在 App Store Connect 更新審核說明：

### 英文版本

```
Testing Instructions:

This app requires a Spotify Premium account for normal use.

FOR REVIEW: We have implemented a Demo Mode specifically for the review process.

How to Test:
1. Launch the app
2. You will see an ORANGE button in the center of the screen labeled:
   "🎭 Demo Mode (For Review)"
3. Tap this ORANGE button (NO login required)
4. The app will enter Demo Mode immediately
5. An orange "🎭 Demo Mode 🎭" badge will appear at the top to confirm
6. All features are now fully accessible for testing

Note: 
- The Demo Mode button is ONLY visible in the review build
- No Spotify account or login is required
- All app features can be tested in Demo Mode

Please feel free to contact us if you have any questions.
Thank you!
```

### 中文版本

```
測試說明：

本 App 正常使用需要 Spotify Premium 付費帳號。

審核測試：我們特別為審核流程實作了 Demo 模式。

測試步驟：
1. 啟動 App
2. 在頁面中央會看到橘色按鈕：
   「🎭 Demo Mode (For Review)」
3. 點擊此橘色按鈕（無需登入）
4. App 會立即進入 Demo 模式
5. 頂部會顯示橘色「🎭 Demo Mode 🎭」標籤確認已進入
6. 現在可以完整測試所有功能

注意事項：
- Demo Mode 按鈕僅在審核版本中顯示
- 不需要 Spotify 帳號或任何登入
- 所有功能都可在 Demo 模式中測試

如有任何問題，請隨時聯繫我們。
謝謝！
```

---

## 🚀 提交審核流程

### 1. 清理建置

```bash
Product → Clean Build Folder (⇧⌘K)
```

### 2. 確認 Scheme

```bash
確認頂部工具欄顯示：MyPlaylist (AppStore Review)
```

### 3. Archive

```bash
Product → Archive (⌃⌘B)
```

### 4. 驗證 Archive

```bash
Window → Organizer
1. 選擇最新的 Archive
2. 點擊「Validate App」
3. 確認無錯誤
```

### 5. 上傳到 App Store Connect

```bash
1. 點擊「Distribute App」
2. 選擇「App Store Connect」
3. 上傳
4. 等待處理完成（約 10-30 分鐘）
```

### 6. 在 App Store Connect 提交審核

```bash
1. 登入 App Store Connect
2. 選擇您的 App
3. 建立新版本或更新現有版本
4. 選擇剛上傳的 Build
5. 填寫更新的審核說明（使用上面的範本）
6. 設定為「手動發布」⚠️
7. 提交審核
```

---

## ⚠️ 重要檢查清單

提交前請確認：

- [ ] 使用 **MyPlaylist (AppStore Review)** Scheme
- [ ] Clean Build Folder (⇧⌘K)
- [ ] 在模擬器/真機測試審核版本
- [ ] 確認左上角**無綠色登入按鈕**
- [ ] 確認中央**有橘色 Demo Mode 按鈕**
- [ ] 測試 Demo Mode 可正常進入
- [ ] Archive 成功
- [ ] Validate 無錯誤
- [ ] 更新審核說明（強調橘色按鈕）
- [ ] 設定為**手動發布**

---

## 🎉 預期結果

修改後的審核版本：

```
┌─────────────────────────────────┐
│  Home                    🔄     │ ← toolbar（左上角空白，右上角是刷新）
├─────────────────────────────────┤
│                                 │
│         🎵                      │
│                                 │
│   Please log in to Spotify     │
│   Log in to view your          │
│   music data                   │
│                                 │
│  ┌───────────────────────────┐ │
│  │ 🎭 Demo Mode (For Review) │ │ ← 橘色按鈕（唯一的登入選項）
│  └───────────────────────────┘ │
│                                 │
│                                 │
└─────────────────────────────────┘
│  🏠   🔍   📊   ⚙️             │
└─────────────────────────────────┘
```

**關鍵改進**：
- ❌ 移除了容易混淆的綠色登入按鈕
- ✅ 只剩下一個明確的橘色 Demo Mode 按鈕
- ✅ 審核員不可能點錯按鈕

---

## 📞 問題排查

### Q: 審核版本左上角還是顯示綠色按鈕？

```bash
解決方法：
1. 確認當前 Scheme 是 "MyPlaylist (AppStore Review)"
2. Product → Clean Build Folder (⇧⌘K)
3. 重新運行
4. 如果還有問題，檢查 Build Settings:
   - 搜尋 "Other Swift Flags"
   - 確認 AppStoreReview 配置有：-D APPSTORE_REVIEW
```

### Q: 正式版本沒有登入按鈕？

```bash
解決方法：
1. 確認當前 Scheme 是 "MyPlaylist"（不是 AppStore Review）
2. Product → Clean Build Folder (⇧⌘K)
3. 重新運行
```

### Q: Demo Mode 按鈕沒有顯示？

```bash
解決方法：
1. 確認使用 MyPlaylist (AppStore Review) Scheme
2. 檢查 HomeView.swift 中的 #if APPSTORE_REVIEW 代碼
3. Clean Build Folder 並重新運行
```

---

## 📚 相關文件

- [DEMO_MODE_README.md](./DEMO_MODE_README.md) - Demo 模式完整說明
- [APP_STORE_審核說明.md](./APP_STORE_審核說明.md) - 審核說明範本
- [XCODE_設定指南.md](./XCODE_設定指南.md) - Xcode 配置教學

---

**版本**：1.0.1  
**修改日期**：2025-11-12  
**狀態**：✅ 已修復，待驗證

